spring:
  application:
    name: order-food
  
  # 数据库配置
  datasource:
    url: jdbc:mysql://sh-cynosdbmysql-grp-arazbxi2.sql.tencentcdb.com:20381/order_food?useUnicode=true&characterEncoding=utf8&useSSL=true&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: T7TeSCXz
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  # JPA 配置
  jpa:
    hibernate:
      # ddl-auto: update  # 开发环境可以使用，生产环境建议禁用
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
    open-in-view: false

  ai:
    deepseek:
      api-key: sk-5510b99550a3499198758497c12e8626
      chat:
        options:
          model: deepseek-chat

# 微信小程序配置
wechat:
  miniapp:
    # 请替换为你的实际 appId 和 appSecret
    app-id: your-app-id-here
    app-secret: your-app-secret-here

# 日志配置
logging:
  level:
    weixin.order_food: DEBUG
    org.springframework.web: INFO
    com.baomidou.mybatisplus: DEBUG